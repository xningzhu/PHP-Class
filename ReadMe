-安裝 fzaninotto/faker 套件，為專案使用的資料表產生假資料。
> composer require --dev fzaninotto/faker
*--dev 表示安裝專為開發環境(開發階段)使用的。

-安裝 doctrine/dbal 套件，才能修改資料表欄位，一樣在專案目錄底下。
> composer require doctrine/dbal

-安裝 database 套件
> composer require illuminate/database

-安裝 date time 套件，要先切到專案目錄底下
> composer require nesbot/carbon


-初始化套件
用 composer 初始化建立一個專案，會要你填寫一些資料
> vagrant.exe ssh
> cd Code/public
> composer init

